# Copy to deploy/.env or use as --env-file for Docker

# Flask server
HOST=0.0.0.0
PORT=3000
FLASK_ENV=production

SECRET_KEY=change-me-to-a-random-string

# SQLite db path (inside container use /data)
DATABASE_PATH=database.db
